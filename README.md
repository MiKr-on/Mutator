# This is a small, straighforward script, that autamatically detects Lys and Arg residues in a .pdb file and changes them to Dab and Dpr aminoacids respectively

# Usage, simply use flags:
> python mutator.py -i foo.pdb -o bar.pdb
